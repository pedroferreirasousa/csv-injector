import styles from './page.module.scss';

export default function Home() {
  return (
    <main className={styles.container}>
      <h1>CSV Injector Dashboard</h1>
      <p>Pronto para começar a construir a interface do projeto!</p>
    </main>
  );
}